import { BarometerStatePipe } from './barometer-state.pipe';

describe('BarometerStatePipe', () => {
  it('create an instance', () => {
    const pipe = new BarometerStatePipe();
    expect(pipe).toBeTruthy();
  });
});
